namespace AtaUiToolkit
{
    public enum ModalType
    {
        NoAction, // TODO does not really exist. Need to move buttons that are already defined in other places to modal implementation.
        SuccessOK,
        OkCancel,
        WarningOK,
        ErrorOK,
        YesNo
    }
}